<article class="setores{{ end ? ' g-d-l' : '' }}">
    <h1>{{ titulo|trans }}</h1>
    <p>
        <strong class="txt-color-orange caps">{{ info.headquarter.cidade }}</strong><br>
        <strong class="caps">{{ info.headquarter[key].nome }}</strong><br>
        <a class="white caps" href="mailto:{{ info.headquarter.negocios.email }}">{{ info.headquarter[key].email }}</a>
    </p>
    <div class="mar-10">...</div>
    <p>
        <strong class="txt-color-orange caps">{{ info.filial.cidade }}</strong><br>
        <strong class="caps">{{ info.filial[key].nome }}</strong><br>
        <a class="white caps" href="mailto:{{ info.filial.negocios.email }}">{{ info.filial[key].email }}</a>
    </p>
</article>